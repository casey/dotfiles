#!/usr/bin/env bash

set -euo pipefail

base="${1:-master}"

if ! git diff-index --quiet HEAD --; then
  echo "error: uncommitted changes"
  exit 1
fi

git fetch origin "$base":"$base"

git read-tree "$base"
