#!/usr/bin/env bash

set -euo pipefail

cargo metadata --format-version=1 \
  | jq \
    --arg dependency "$2" \
    --raw-output \
    '.packages[] | select(.name==$dependency) | .manifest_path' \
  | xargs dirname
