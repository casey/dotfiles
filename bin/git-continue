#!/usr/bin/env bash

set -euo pipefail

continue() {
  if git rev-parse --verify $1 > /dev/null 2> /dev/null; then
    echo "Continuing $2…"
    git $2 --continue
  fi
}

continue CHERRY_PICK_HEAD cherry-pick

continue MERGE_HEAD merge

continue REBASE_HEAD rebase

continue REVERT_HEAD revert
