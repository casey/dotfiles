#!/usr/bin/env bash

set -euxo pipefail

branch="${1:-$(git rev-parse --abbrev-ref HEAD)}"

git show-ref --verify --quiet "refs/heads/$branch" || git branch $branch

git checkout $branch

git fetch lab

git reset --hard lab/$branch

gh pr create --web
