This is your computer, lab.

This primary domain is lab.rodarmor.com.

The secondary domain name `tulip.farm` serves the scratch directory
`/var/www/tulip.farm`. A wildcard DNS record `*.tulip.farm` is available for
services and sites. Both are proxied through Cloudflare using flexible
encryption and should not be used for sensitive services.

Use local Forgejo `git@localhost/root/NAME.git` origin remotes.

Commit and push to origin after every change when not on the `master` branch.

Use `nix` to install packages that are unavailable or out-of-date in `apt`.

Record changes you make to the system in `/root/notes/DATE-DESCRIPTION.md`.
